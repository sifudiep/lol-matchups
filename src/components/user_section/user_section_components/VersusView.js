import React, { Component } from "react";
import { connect } from "react-redux";

class VersusView extends Component {
  render() {
    return <div />;
  }
}

const mapStateToProps = state => {
  return {};
};

export default connect(mapStateToProps)(VersusView);
